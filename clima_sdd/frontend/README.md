# Frontend

La capa de presentación está en `frontend/templates/` y `frontend/static/`. Contiene las vistas HTML de login, registro y consulta, además de los estilos responsive.

Si la interfaz crece, esta carpeta es el lugar destinado para migrar a un frontend separado (por ejemplo, React/Vue) sin mezclarlo con `backend/`.
